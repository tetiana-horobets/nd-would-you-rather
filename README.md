This is a simple polling application, where you can choose from two options.

## Available Scripts

To run the app execute:

`npm install`
`npm start`

Open [http://localhost:3000](http://localhost:3000) to view it in the browser.
